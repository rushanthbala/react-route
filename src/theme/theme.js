import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#4DB6AC',
      contrastText:'#FFFFFF'
    },
    secondary: {
      main: '#70cf66'
    },
  }
});

export default theme;
